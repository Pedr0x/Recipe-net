Stuff  I need to fix

--Turn XHR request into a promise
-- Properly tab code
-- Apply ternary operators right
--Delete JavaScript Styles
--Take unnecesary data from React´s state
---Delete unnecesary variables
--Properly use arrow functions and delete the unnecesary ones


Stuff I need to add
--Infinite scrolling 

 if(isMakingRequest == false){
		 	isMakingRequest = true;
			 props.showMoreResults();
			 setTimeout(() => {isMakingRequest = false; console.log("timeout ended")}, 2550);
		 }
		 else if (isMakingRequest == true) {
				console.log("already making req")
			}
			
			
14 updates